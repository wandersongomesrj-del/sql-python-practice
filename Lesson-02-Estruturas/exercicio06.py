### Exercício 6 – Tipos Básicos: Int e Float no Financeiro
"""
Um analista financeiro está criando um controle de contratos para uma microempresa. O cenário exige a distinção clara entre números inteiros (contagem de contratos) e números 
decimais (valores monetários) para garantir precisão contábil. O programa deve armazenar esses dados e confirmar os tipos de dados processados.

**Tarefa**

Crie as variáveis `contratos = 12` e `faturamento = 2500.50`. Use a função `print()` para exibir o tipo da variável `contratos` e o tipo da variável `faturamento` utilizando a 
função `type()`.
"""

contratos = 12
faturamento = 2500.50
print("Contratos: ", contratos)
print("Faturamento: ", faturamento)
print(type(contratos))
print(type(faturamento))
